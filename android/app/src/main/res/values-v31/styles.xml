<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- On Android 12+, the OS shows its own splash screen using the app icon
         before Flutter draws. We leave it blank (background color only, no
         icon) so the app jumps straight into our own branded splash screen
         (logo + title + spinner) as soon as Flutter's first frame is ready,
         instead of showing a separate icon-only frame first. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@android:color/transparent</item>
        <item name="android:windowSplashScreenBrandingImage">@null</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
